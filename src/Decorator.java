public class Decorator implements Component{

    Component component;

    public Decorator(Component newComponent){
    this.component = newComponent;
}
    @Override
    public String operation() {
        return operation();
    }
}
